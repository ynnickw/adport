## What & why

## Checklist

- [ ] Commits are signed off (`git commit -s`, DCO)
- [ ] `pnpm build && pnpm test` passes
- [ ] New write tools go through `guardedWriteTool` / the policy engine (no bypasses)
- [ ] Tool descriptions stay lean (reference material belongs in schema tools/resources)
